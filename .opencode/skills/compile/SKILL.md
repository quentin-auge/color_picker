---
name: compile
description: Compile Arduino code on ESP32
---
Use `arduino-cli compile -b esp32:esp32:esp32c3` to compile.
If it fails, suggest changes, apply them, and rerun.
Stop after 3 tries max.
